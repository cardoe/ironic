# Copy this file to terraform.tfvars and fill in your values.
#
# Required: cloud_name must match an entry in your clouds.yaml

cloud_name            = "mycloud"
external_network_name = "external"

# Path to the Cisco Nexus 9000v QCOW2 image on your local machine.
# Download from https://software.cisco.com (search "Nexus 9000v").
cisco_9k_image_path = "/path/to/nexus9300v64.10.3.7.M.qcow2"

# Name of an Ubuntu 24.04 image already in the hosting cloud's Glance.
devstack_image_name = "Ubuntu-24.04"

# Flavors - adjust to match your hosting cloud's available flavors.
# DevStack needs significant resources for running all OpenStack services.
devstack_flavor    = "m1.xlarge"    # 8 vCPU, 32 GB RAM, 100 GB disk
cisco_9k_flavor    = "m1.large"     # 2 vCPU, 8 GB RAM per switch (x4)
baremetal_flavor   = "m1.medium"    # Size visible to Ironic as "hardware"
# controller_flavor = ""            # Defaults to baremetal_flavor if empty

# SSH key pair (must exist in the hosting cloud)
key_pair_name = "mykey"

# Number of virtual bare metal nodes.
# Even-indexed nodes (0, 2, ...) attach to leaf01.
# Odd-indexed nodes (1, 3, ...) attach to leaf02.
baremetal_node_count = 2

# Management network (defaults are usually fine)
# mgmt_subnet_cidr   = "192.168.32.0/24"
# controller_mgmt_ip = "192.168.32.254"
# devstack_mgmt_ip   = "192.168.32.20"
# spine01_mgmt_ip    = "192.168.32.11"
# spine02_mgmt_ip    = "192.168.32.12"
# leaf01_mgmt_ip     = "192.168.32.13"
# leaf02_mgmt_ip     = "192.168.32.14"

# VLAN range for tenant networks (must match DevStack local.conf)
# vlan_range_start = 100
# vlan_range_end   = 150

# Switch admin password (used in POAP configs)
# switch_password = "system_s3cret!"
